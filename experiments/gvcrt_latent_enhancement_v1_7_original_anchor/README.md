# GVC-RT V1.7 - Original Anchor

```bash
/Huang_group/zyyz/home_dir/.conda/envs/gvc-rt/bin/python run.py --output results_v1
```

This bounded experiment keeps all networks frozen and optimizes only the existing l3 enhancement code grid. `BASE_ORIGINAL` is the original GVC-RT reconstruction from the current base stream. `ZERO_D_LEGACY` remains the historical same-model zero path used by V1.6 and is not redefined.
